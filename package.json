{
  "name": "rugo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RUGO Three.js prototype packaged for Android with Capacitor.",
  "scripts": {
    "serve": "php -S localhost:8080",
    "android:prepare": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/prepare-android-web.ps1",
    "android:add": "pnpm run android:prepare && cap add android",
    "android:sync": "pnpm run android:prepare && cap sync android",
    "android:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}
