{
  "name": "rugo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RUGO Three.js prototype packaged for Android with Capacitor.",
  "scripts": {
    "serve": "php -S localhost:8080",
    "android:add": "npx cap add android",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}
